"use strict";
// Void será usado quando a função não tiver nenhum retorno.
// exemplo - Funções 
const logError = (erroMessage) => {
    console.log(erroMessage);
};
logError('Required filed- Name!');
